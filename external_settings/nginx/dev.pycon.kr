upstream app_server {
        server unix:///var/run/uwsgi/app/dev.pyconkr/socket;
}

server {
        listen 80;

        root /usr/share/nginx/html;

        # Make site accessible from http://localhost/
        server_name dev.pycon.kr;

        location / {
                uwsgi_pass app_server;
                include uwsgi_params;
        }
        location /media {
                alias /home/deploy/dev.pyconkr/media/;
        }
        location /static {
                alias /home/deploy/dev.pyconkr/collected_static/;
        }
}
